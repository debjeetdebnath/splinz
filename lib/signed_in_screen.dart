import 'package:flutter/material.dart';

class SignedIn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Google SignedIn Successfully'),
      ),
      body: SafeArea(
        child: Column(
        ),
      ),
    );
  }
}
